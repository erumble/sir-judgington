%pre
  "Entry","Name","Character","IP"
  - @entries.each do |entry|
    - entry.cosplays.each do |cp|
      "#{entry.entry_num}","#{cp.person_full_name.titleize}","#{cp.character_name.titleize}","#{cp.character_property.titleize}"</br>
